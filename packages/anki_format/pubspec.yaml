name: anki_format
description: Parses .apkg archives and maps Anki's legacy collection schema, note types, and media into a portable model.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.13.2
resolution: workspace

dev_dependencies:
  test: ^1.25.6
  very_good_analysis: ^11.0.0
