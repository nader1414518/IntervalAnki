/// Parses `.apkg` archives and maps Anki's legacy collection schema, note
/// types, and media into a portable model this app's Drift schema can import.
///
/// The archive/SQLite parsing and schema mapping land in milestone M7 of the
/// implementation plan.
library;
